
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Leisti vartotojui skaityti ir rašyti (kurti, atnaujinti) tik į savo paties dokumentą
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
